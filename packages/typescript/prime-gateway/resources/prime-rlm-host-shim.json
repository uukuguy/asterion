{
  "format": "asterion.prime-rlm-host-shim/v1",
  "patches": [
    {
      "anchor": "  state.runtime.setSubagentRuntimeHost(callbacks.subagentRuntimeHost);",
      "replacement": "  const asterionPrimeRlmHostBinding = callbacks.subagentRuntimeHost;\n  const asterionPrimeRlmHostPath = require(\"node:path\").join(state.runtime.services.agentDir, \"asterion-rlm-host-shim.mjs\");\n  const asterionPrimeRlmHostDiscoveryPath = require(\"node:path\").join(state.runtime.services.agentDir, \"asterion-rlm-host.json\");\n  const { createRlmHostClient: asterionCreateRlmHostClient, wrapSubagentRuntimeHost: asterionWrapSubagentRuntimeHost } = await import(require(\"node:url\").pathToFileURL(asterionPrimeRlmHostPath).href);\n  const asterionPrimeRlmHostClient = await asterionCreateRlmHostClient(asterionPrimeRlmHostDiscoveryPath);\n  state.runtime.setSubagentRuntimeHost(asterionWrapSubagentRuntimeHost(asterionPrimeRlmHostBinding, asterionPrimeRlmHostClient, asterionPrimeRlmHostClient.hostContext));"
    },
    {
      "anchor": "    };\n    try {\n      const { status } = await this.withAgentMessageTargetLock(targetState.activeSessionId, async () => {",
      "replacement": "    };\n    if (options.fromState?.runtime.metadata.rlmChildId || targetState.runtime.metadata.rlmChildId) {\n      const asterionPrimeRlmMessagePath = require(\"node:path\").join(targetState.runtime.services.agentDir, \"asterion-rlm-host-shim.mjs\");\n      const asterionPrimeRlmMessageDiscoveryPath = require(\"node:path\").join(targetState.runtime.services.agentDir, \"asterion-rlm-host.json\");\n      const { createRlmHostClient: asterionCreateRlmMessageClient, admitNativeRlmMessage: asterionAdmitNativeRlmMessage, recordNativeRlmMessageDelivered: asterionRecordNativeRlmMessageDelivered } = await import(require(\"node:url\").pathToFileURL(asterionPrimeRlmMessagePath).href);\n      const asterionPrimeRlmMessageClient = await asterionCreateRlmMessageClient(asterionPrimeRlmMessageDiscoveryPath);\n      const asterionPrimeRlmMessageIdentity = (state2) => state2?.runtime.metadata.rlmChildId ?? asterionPrimeRlmMessageClient.parentSessionId;\n      await asterionAdmitNativeRlmMessage(asterionPrimeRlmMessageClient, payload, { sender_id: asterionPrimeRlmMessageIdentity(options.fromState), recipient_id: asterionPrimeRlmMessageIdentity(targetState) });\n      const asterionPrimeRlmMessageDelivery = targetState.runtime.session.waitForAgentMessagePromptDelivery(payload.id);\n      void asterionPrimeRlmMessageDelivery.then(() => asterionRecordNativeRlmMessageDelivered(asterionPrimeRlmMessageClient, payload.id)).catch(() => undefined);\n    }\n    try {\n      const { status } = await this.withAgentMessageTargetLock(targetState.activeSessionId, async () => {"
    }
  ],
  "target": "packages/coding-agent/dist/bundle/chunk-UGFTGOX2.js"
}
