[project]
name = "asterion-control-skill"
version = "0.1.0"
description = "Private Asterion control bridge for Prime Agent"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/asterion_control"]
