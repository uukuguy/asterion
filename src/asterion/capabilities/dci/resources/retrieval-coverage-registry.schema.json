{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "dci.retrieval-coverage-registry/v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "dataset_id", "selected_ids_sha256", "manifests"],
  "properties": {
    "schema": {"const": "dci.retrieval-coverage-registry/v1"},
    "dataset_id": {"type": "string", "minLength": 1},
    "selected_ids_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "manifests": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["query_sha256", "path", "sha256"],
        "properties": {
          "query_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
          "path": {"type": "string", "minLength": 1, "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$))(?!.*\\\\).+$"},
          "sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
        }
      }
    }
  }
}
